type CardAboutProps = {
    title: string;
    description: string;
    logo: string;
}

const CardAbout = ({ title, description, logo }: CardAboutProps) => {
    return (
        <div></div>
    )
}

export default CardAbout;